sudo: required
dist: trusty
language: cpp
notifications:
  email: false

python:
  #- "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"

os:
  - linux
before_install:
  - sudo add-apt-repository -y ppa:beineri/opt-qt57-trusty

install:
  - sudo pip install pywinauto
