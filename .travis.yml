sudo: required
dist: trusty
language: cpp
notifications:
  email: false

os:
  - linux
before_install:
  - sudo add-apt-repository -y ppa:beineri/opt-qt57-trusty

install:
  - sudo pip install pywinauto
